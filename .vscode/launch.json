{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pi x IDE VS Code Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/ide-plugins/vscode", "${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/ide-plugins/vscode/out/extension.js"],
      "preLaunchTask": "mise: build"
    }
  ]
}
