#!/usr/bin/env bash
#MISE description="Build and run unit tests"
#MISE depends=["setup"]
set -euo pipefail

mise run --skip-deps check:config-schema
mise run --skip-deps compile
node --test dist/test/*.test.js
